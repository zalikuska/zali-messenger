<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Screen-share (START_SCREEN_CAPTURE in NativeBridge.kt, see
         ScreenCaptureService) — Android 14+ (targetSdk 35) requires both the
         foreground-service permission and the mediaProjection-specific one
         before MediaProjection.createVirtualDisplay() will succeed. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!--
        Auto Backup ВЫКЛЮЧЕН намеренно, и это не перестраховка.

        В filesDir лежат `conversation_keys_<user>.json` (все ключи переписок
        открытым текстом) и `shared_device_identity_<user>.json` (приватный
        ECDH-ключ устройства). При allowBackup="true" Android без спроса выгружает
        этот каталог в Google Drive пользователя — то есть ключевой материал
        сквозного шифрования уезжает третьей стороне, ровно тому наблюдателю, от
        которого шифрование и защищает. Ни macOS, ни iOS, ни Windows такого канала
        не имеют.

        Восстановление ключей на новом устройстве закрыто своим механизмом —
        cloud vault под парольной фразой (см. web/src/interface/conversation_keys.js),
        где сервер видит только зашифрованный конверт.
    -->
    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.ZaliMessenger">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.ZaliMessenger">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Hosts the MediaProjection/VirtualDisplay capture loop for screen
             sharing (START_SCREEN_CAPTURE). Must be a foreground service with
             this exact type on API 34+ or createVirtualDisplay() throws. -->
        <service
            android:name=".ScreenCaptureService"
            android:foregroundServiceType="mediaProjection"
            android:exported="false" />

        <!-- Фоновые пуши FCM (server/src/fcm.rs): будит приложение, расшифровывает
             сообщение ключами с устройства и показывает уведомление. -->
        <service
            android:name=".PushMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- DOWNLOAD_ATTACHMENT (NativeBridge.kt's saveAttachment) shares attachments
             via a content:// Uri through the system share sheet, mirroring iOS's
             UIActivityViewController. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
